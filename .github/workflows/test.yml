name: Simple Test

on:
  push:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        
      - name: Test step
        run: |
          echo "Hello World!"
          ls -la
          echo "Test completed successfully!"
